import type { NextPage } from 'next';
import { UserSettingProfile } from '../../src/features/User';

const SettingsProfile: NextPage = () => {
  return <UserSettingProfile />;
};

export default SettingsProfile;
